## meow[note]

### developing

#### testing

the tests are in [`shared/test`](shared/test), [`shared/test@jvm`](shared/test@jvm) and [`shared/test@android`](shared/test@android)

do this to run them :

```bash
./amper test
```