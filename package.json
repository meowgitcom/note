{
  "name": "opennote",
  "private": true,
  "workspaces": ["api", "electron", "env", "expo"],
  "scripts": {
    "dev": "cd electron && bun dev",
    "expo": "cd expo && bun dev",
    "fix": "cd expo && bun fix && cd ../electron && bun fix"
  }
}
