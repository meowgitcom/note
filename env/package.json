{
  "name": "env",
  "private": true,
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "fix": "oxfmt -c ../config/oxfmt.json && oxlint -c ../config/oxlint.json --fix && tsc --noEmit",
    "setup": "bun setup.ts"
  },
  "dependencies": {
    "@t3-oss/env-core": "^0.13.11",
    "valibot": "^1.3.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
