{
  "name": "api",
  "private": true,
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "fix": "oxfmt -c ../config/oxfmt.json && oxlint -c ../config/oxlint.json --fix && tsc --noEmit"
  },
  "dependencies": {
    "elysia": "^1.4.28"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxfmt": "^0.44.0",
    "oxlint": "^1.59.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
