product: js/app

dependencies:
  - $ktor.server.core
  - $ktor.server.contentNegotiation
  - $ktor.serialization.kotlinx.json

settings:
  ktor:
    enabled: true
  kotlin:
    optIns: [ kotlin.js.ExperimentalJsExport ]